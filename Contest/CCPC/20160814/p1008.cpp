#include<cstdio>
#include<cstring>
#include<cstdlib>
#include<cmath>
#include<algorithm>
using namespace std;

int main() {
    int TT, T;
    scanf("%d", &TT);
    for (T = 1; T <= TT; ++T) {
        scanf("%d", &n);
        for (i = 1; i <= n; ++i)
    }
    return 0;
}