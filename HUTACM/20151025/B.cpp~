#include<cstdio>
#include<cstring>
#include<algorithm>

using namespace std;

int gcd(int a,int b){
  if (a%b==0) return b;
  return gcd(b,a mod b);
}


int main() {
  int ans,i,n;
  scanf("%d",&n);
  ans=0;
  for (i=1;i<=n;++i)
    if (gcd(n,i)==1) ++ans;
  printf();
  return 0;
}

